<!DOCTYPE html>
<html>
<head>
    <title>Meaningful Movies</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css"  />
    <link rel="stylesheet" href="/stylesheets/main.css" />
</head>
<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">MeaningfulMovies</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
<!-- we have movies.currentUser  we can use to toggle what links to show -->
                    <% if (!currentUser){ %>
                        <li><a href="/login">Login</a></li>
                        <li><a href="/signup">SignUp</a></li>
                    <% } else { %>
                        <li>
                            <a href="/profile"/> Currently signed in as:
                            <strong><%= currentUser.username %></strong></a>
                        </li>
                        <li><a href="/logout">Logout</a></li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    <% if (error && error.length > 0) { %>
        <div class="container-fluid">
            <div class="alert alert-danger" role="alert"><%= locals.error %></div>
        </div>
    <% } %>
    <% if (success && success.length > 0) { %>
        <div class="container-fluid">
            <div class="alert alert-success" role="alert"><%= locals.success %></div>
        </div>
    <% } %>
